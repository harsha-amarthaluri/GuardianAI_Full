# Guardian AI — Infrastructure & Container Deployment

## Overview
Contains Docker, Compose, and deployment infrastructure definitions for Guardian AI microservices.

- `docker-compose.yml` (located in workspace root) provides local multi-container development environment.
- Environment secrets are configured via `.env` (copied from `.env.example`).
